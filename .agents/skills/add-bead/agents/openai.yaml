# Optional Codex skill metadata. Generated from Claude Code slash command /add-bead.
# This mirrors an explicit command, so it does NOT fire implicitly — invoke it with: $add-bead
policy:
  allow_implicit_invocation: false
