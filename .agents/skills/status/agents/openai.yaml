# Optional Codex skill metadata. Generated from Claude Code slash command /status.
# This mirrors an explicit command, so it does NOT fire implicitly — invoke it with: $status
policy:
  allow_implicit_invocation: false
