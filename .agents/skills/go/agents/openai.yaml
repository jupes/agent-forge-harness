# Optional Codex skill metadata. Generated from Claude Code slash command /go.
# This mirrors an explicit command, so it does NOT fire implicitly — invoke it with: $go
policy:
  allow_implicit_invocation: false
