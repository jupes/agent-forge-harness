# Optional Codex skill metadata. Generated from Claude Code slash command /forgemaster.
# This mirrors an explicit command, so it does NOT fire implicitly — invoke it with: $forgemaster
policy:
  allow_implicit_invocation: false
