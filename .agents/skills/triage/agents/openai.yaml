# Optional Codex skill metadata. Generated from Claude Code slash command /triage.
# This mirrors an explicit command, so it does NOT fire implicitly — invoke it with: $triage
policy:
  allow_implicit_invocation: false
