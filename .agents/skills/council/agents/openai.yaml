# Optional Codex skill metadata. Generated from Claude Code slash command /council.
# This mirrors an explicit command, so it does NOT fire implicitly — invoke it with: $council
policy:
  allow_implicit_invocation: false
