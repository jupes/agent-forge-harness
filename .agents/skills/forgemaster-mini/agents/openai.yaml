# Optional Codex skill metadata. Generated from Claude Code slash command /forgemaster-mini.
# This mirrors an explicit command, so it does NOT fire implicitly — invoke it with: $forgemaster-mini
policy:
  allow_implicit_invocation: false
