# Optional Codex skill metadata. Generated from Claude Code slash command /ship.
# This mirrors an explicit command, so it does NOT fire implicitly — invoke it with: $ship
policy:
  allow_implicit_invocation: false
