# Optional Codex skill metadata. Generated from Claude Code slash command /sync-knowledge.
# This mirrors an explicit command, so it does NOT fire implicitly — invoke it with: $sync-knowledge
policy:
  allow_implicit_invocation: false
