# Optional Codex skill metadata. Generated from Claude Code slash command /review.
# This mirrors an explicit command, so it does NOT fire implicitly — invoke it with: $review
policy:
  allow_implicit_invocation: false
